DLLs needed if Microsoft compiler is used

MWetter@lbl.gov                   2008-02-20
############################################

This directory contains dlls that
are required if the Microsoft compiler
is used. If this libraries cannot be found,
then the program cclient.exe crashes
if run from cygwin with no warning.

For cygwin, they are not needed.